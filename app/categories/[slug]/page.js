import Link from"next/link";import{notFound}from"next/navigation";import{getAllArticles,getCategories}from"@/lib/articles";export function generateStaticParams(){return getCategories().map(c=>({slug:c.slug}))}export default async function Page({params}){const{slug}=await params;const c=getCategories().find(x=>x.slug===slug);if(!c)notFound();const items=getAllArticles().filter(a=>(a.category||"Marketing Strategy").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")===slug);return <div className="container"><div className="pagehead"><div className="kicker">Category</div><h1>{c.name}</h1></div><div className="article-grid">{items.map(a=><Link className="card article-card" key={a.slug} href={`/articles/${a.slug}`}><h2 style={{fontSize:25}}>{a.title}</h2><p className="muted">{a.description}</p></Link>)}</div></div>}
