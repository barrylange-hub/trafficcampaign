import Link from"next/link";import{getCategories}from"@/lib/articles";export const metadata={title:"Marketing Categories"};export default function Page(){return <div className="container"><div className="pagehead"><div className="kicker">Browse</div><h1>Categories</h1></div><div className="simple-list">{getCategories().map(c=><Link href={`/categories/${c.slug}`} key={c.slug}>{c.name} <span className="muted">({c.count})</span></Link>)}</div></div>}
