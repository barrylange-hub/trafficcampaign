import{SITE}from"@/lib/site";import{getAllArticles}from"@/lib/articles";export function GET(){const articles=getAllArticles();const blocks=[`# ${SITE.name}`,``, `> ${SITE.description}`,``,`Canonical site: ${SITE.url}`,``,`# Published Content`,``,...articles.flatMap(a=>[`## ${a.title}`,`URL: ${SITE.url}/articles/${a.slug}`,`Category: ${a.category}`,`Published: ${a.date}`,`Description: ${a.description}`,``,a.body,``])];return new Response(blocks.join("\n"),{headers:{"Content-Type":"text/plain; charset=utf-8","Cache-Control":"public, max-age=300"}})}
