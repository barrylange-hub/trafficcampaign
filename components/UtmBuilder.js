"use client";
import {useMemo,useState} from "react";
export default function UtmBuilder(){const[url,setUrl]=useState("https://example.com/");const[source,setSource]=useState("google");const[medium,setMedium]=useState("cpc");const[campaign,setCampaign]=useState("summer-sale");const output=useMemo(()=>{try{const u=new URL(url);if(source)u.searchParams.set("utm_source",source);if(medium)u.searchParams.set("utm_medium",medium);if(campaign)u.searchParams.set("utm_campaign",campaign);return u.toString()}catch{return "Enter a valid URL above."}},[url,source,medium,campaign]);return <div className="card calc"><h2 style={{fontSize:25,marginTop:0}}>UTM Builder</h2><div className="field"><label>Destination URL</label><input value={url} onChange={e=>setUrl(e.target.value)}/></div><div className="field"><label>Source</label><input value={source} onChange={e=>setSource(e.target.value)}/></div><div className="field"><label>Medium</label><input value={medium} onChange={e=>setMedium(e.target.value)}/></div><div className="field"><label>Campaign</label><input value={campaign} onChange={e=>setCampaign(e.target.value)}/></div><div className="result"><span className="muted">Campaign URL</span><div style={{overflowWrap:'anywhere',fontWeight:800,marginTop:6}}>{output}</div></div></div>}
